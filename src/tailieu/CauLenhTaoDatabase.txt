create database librarytesting
go 
use librarytesting
go
create table book(
    bookid int primary key,
	nameBook nvarchar(255),
	description nvarchar(255),
	type nvarchar(255),
	writing nvarchar(255),
	quantity int ,
	yearRelease int
)

create table Users(
    userID int primary key,
	name nvarchar(255),
	phone nvarchar(255),
	address nvarchar(255),
	email nvarchar(255),
	gender nvarchar(255) ,
	age int
)

create table transactions(
     transactionId int primary key,
     startDay int,
     endDay int,
     quantity int,
     bookid int FOREIGN KEY REFERENCES book (bookid),
     userID int FOREIGN KEY REFERENCES Users(userID)
)
create table resume(
     bookid int FOREIGN KEY REFERENCES book (bookid),
     userID int FOREIGN KEY REFERENCES Users(userID),
	 transactionId int FOREIGN KEY REFERENCES transactions(transactionId),
	 status nvarchar(255),
	 resumeid int primary key
 )


create table librarian(
    libID int primary key,
	name nvarchar(255),
	phone nvarchar(255),
	address nvarchar(255),
	email nvarchar(255),
	password nvarchar(255),
	gender nvarchar(255) 
 )

 -- 
 insert into Users(userID,name,phone,address,email,gender,age)
 values ('1','name1','phone1','address1','email1','gioitinh1',1)
  insert into Users(userID,name,phone,address,email,gender,age)
 values ('2','name2','phone2','address2','email2','gioitinh2',2)
  insert into Users(userID,name,phone,address,email,gender,age)
 values ('3','name3','phone3','address3','email3','gioitinh3',3)
  insert into Users(userID,name,phone,address,email,gender,age)
 values ('4','name4','phone4','address4','email4','gioitinh4',4)
  insert into Users(userID,name,phone,address,email,gender,age)
 values ('5','name5','phone5','address5','email5','gioitinh5',5)
  insert into Users(userID,name,phone,address,email,gender,age)
 values ('6','name6','phone6','address6','email6','gioitinh6',6)